SSH_DIR=/home/core/.ssh
AUTH_KEYS="$SSH_DIR/authorized_keys"
PUBKEY=/etc/id_ed25519.pub

mkdir -p "$SSH_DIR"
chmod 700 "$SSH_DIR"

cat "$PUBKEY" >> "$AUTH_KEYS"

chmod 600 "$AUTH_KEYS"
chown -R core:core "$SSH_DIR"